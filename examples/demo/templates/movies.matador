<!DOCTYPE html>
<html lang="en">
{% include "../templates/header.matador" %}
<body>
    <div class="header">
        <h1>Page Title</h1>
        <h2>A subtitle for your page goes here</h2>
    </div>

    <div class="content">
        <div class="pure-g">
            <div class="pure-u-1">
                <table class="pure-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Year</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for movie in movies %}
                        <tr>
                            <td>{{ movie.id }}</td>
                            <td>{{ movie.title }}</td>
                            <td>{{ movie.year }}</td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>